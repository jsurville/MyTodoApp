import { Observable } from 'rxjs';
export declare function isNotNullable<T>(it: T): it is NonNullable<T>;
export declare const filterNullable: <T>() => (source: Observable<T>) => Observable<NonNullable<T>>;
